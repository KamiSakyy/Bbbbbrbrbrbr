<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Никаких градиентов/теней: только плоский чёрный фон и системные панели. -->
    <style name="Theme.YandexBrowser" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/black_bg</item>
        <item name="android:colorBackground">@color/black_bg</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentTransitions">false</item>
    </style>
</resources>
